{%- render 'spacing-style', settings: section.settings -%}
   <nav aria-label="Breadcrumb">
     {%- render 'pdp-breadcrumb', product: product -%}
   </nav>

{% schema %}
{
  "name": "PDP Breadcrumb",
  "tag": "section",
  "class": "pdp-breadcrumb-section",
  "settings": [
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Top padding",
      "min": 0,
      "max": 60,
      "step": 4,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Bottom padding",
      "min": 0,
      "max": 60,
      "step": 4,
      "unit": "px",
      "default": 0
    }
  ],
  "enabled_on": {
    "templates": ["product"]
  },
  "presets": [
    {
      "name": "PDP Breadcrumb"
    }
  ]
}
{% endschema %}
